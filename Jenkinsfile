pipeline {
  agent any 
  
  stages {
    stage ('Compile Stage') {
      steps { 
        echo 'Running compile stage'
      }
    }
    
   stage ('Deployment Stage') {
     steps { 
      echo 'Running deployment stage'
     }
    }
  }
}
