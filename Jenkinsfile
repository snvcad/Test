pipeline {
  agent any
  
  stages {
    stage ('Compile Stage') {
      
      steps { 
        echo 'Running compile stage'
        bat "cd c:\\product"
      }
    }
    
   stage ('Test Stage') {
     steps { 
      echo 'Running Test stage'
     
     }
    }
  }
}
