pipeline {
  agent any
  
  stages {
    stage ('Compile Stage') {
      
      steps { 
        echo 'Running compile stage'
      }
    }
    
   stage ('Test Stage') {
     steps { 
      echo 'Running Test stage'
     
     }
    }
  }
}
