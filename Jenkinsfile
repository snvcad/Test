pipeline {
  agent any 
  
  stages {
    stage ('Compile Stage') {
      
      steps { 
        echo 'Running compile stage'
        bat 'set'
         }
    }
    
   stage ('Test Stage') {
     steps { 
      echo 'Running Test stage'
     
     }
    }
  }
}
